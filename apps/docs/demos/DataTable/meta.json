{ "description": "A feature-rich, controlled data table with typed columns, custom cell rendering, controlled sorting, row selection, sticky headers, loading and empty states, and row-click handling." }
