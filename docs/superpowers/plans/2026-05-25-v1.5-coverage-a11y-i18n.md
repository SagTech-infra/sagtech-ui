# v1.5 — Coverage push + a11y / i18n polish — Implementation Plan

> **For agentic workers:** executed via superpowers:subagent-driven-development (fresh implementer per task + two-stage review: spec compliance, then code quality). TDD; suite green between phases.

**Goal:** Close the long-tail test-coverage gap, fix a11y (focus-visible) + i18n (locale-aware dates, RTL) debts, consolidate the Drawer/Sheet motion tail, migrate high-traffic raw classes to semantic tokens, add missing Storybook stories, and bump the publish CI — additively, **zero breaking changes**.

**Tech Stack:** TypeScript 6, React 19, Tailwind v4 (CSS `@theme`, logical utilities `ps/pe/ms/me/start/end`, `border-s/e`), framer-motion (peer, globally mocked in tests), Vitest + happy-dom + Testing Library, tsup, Storybook 10.

**Source spec:** `docs/superpowers/specs/2026-05-25-v1.5-coverage-a11y-i18n-design.md`

**Invariants:** default `locale`/`dir` = `en-US`/`ltr`; 346 baseline tests stay green between phases; framer-motion stays globally mocked (assert logical state, not animation); never remove tsconfig `ignoreDeprecations: "6.0"`. Tag only after explicit user confirmation.

---

## Phase 0 — Pre-flight (DONE during planning)

- [x] Commit uncommitted Storybook reorg on `release/v1.4.0` (`docs(storybook): normalize sidebar sections — Overlays/3D + storySort`, `92ab241`).
- [x] Branch `release/v1.5.0` off `release/v1.4.0`.
- [x] Write spec + this plan; commit.

## Phase A — CI: publish.yml Node 24 (deadline 2026-06-02)

**Files:** `.github/workflows/publish.yml`
- [ ] `actions/checkout@v4`→`@v5`; `actions/setup-node@v4`→`@v5`; `node-version: 20`→`24`. Keep `pnpm/action-setup@v4` + tag↔version guard.
- [ ] Verify YAML well-formed (no behavior change to install/build/publish).
- [ ] Commit `ci(publish): bump actions to v5 + Node 24 (GitHub Node-20 EOL 2026-06-02)`.

## Phase B — Test infra + render-smoke

### Task B.1 — Global mocks in `vitest.setup.ts`
- [ ] Add `IntersectionObserver` + `ResizeObserver` no-op class stubs.
- [ ] Add `HTMLCanvasElement.prototype.getContext('2d')` no-op 2D stub + `window.devicePixelRatio = 1`.
- [ ] Add global `vi.mock` for `@react-three/fiber` (`Canvas`→`<div data-testid="r3f-canvas">`, children ignored), `@react-three/drei`, `three`, `react-force-graph-3d`.
- [ ] `pnpm test` — 346 still green (per-test `matchMedia` overrides untouched).
- [ ] Commit `test(setup): add IO/RO/canvas/3D global mocks`.

### Task B.2 — Tail smoke (VirtualList / RichTextEditor / VisualGraphEditor)
- [ ] Colocated `__tests__/<Name>.test.tsx`: render + assert mount (rows / toolbar+editor / provider+controls). Commit `test: render-smoke for VirtualList/RichTextEditor/VisualGraphEditor`.

### Task B.3 — Canvas charts smoke (×10)
- [ ] AreaChart, BarChart, HeatmapChart, RadarChart, ScatterChart, GaugeChart, SankeyChart, FunnelChart, SparklineChart, TreemapChart — mount + `<canvas>` w/h + DOM labels. Commit `test: mount-smoke for 10 canvas charts`.

### Task B.4 — 3D suite smoke (×4)
- [ ] Network3D, Globe3D, Scene3D, Mindmap3D — mount no-crash under mocks. Commit `test: render-smoke for 3D suite`.

## Phase C — a11y focus-visible rings

**Files:** `src/components/Input/input.const.ts`, `src/components/Button/button.const.ts`, `src/components/SelectInput/SelectFakeInput.tsx`
- [ ] Add `focus-visible:ring-2 focus-visible:ring-pr_purple` (+offset where clean), following `SegmentedControl`/`TagInput`. Keep `outline-none`.
- [ ] `pnpm test` green; commit `fix(a11y): add theme-aware focus-visible rings to base form controls`.

## Phase D — i18n dates

### Task D.1 — LocaleProvider + useLocale + compose + exports
- [ ] `src/providers/LocaleContext.tsx` (RSC-safe; `Direction`, `LocaleContextValue`, `DEFAULT_LOCALE='en-US'`, `DEFAULT_DIR='ltr'`, `LocaleContext`, `useLocale`).
- [ ] `src/providers/LocaleProvider.tsx` (`'use client'`; controlled-only, memoized).
- [ ] `src/providers/__tests__/LocaleProvider.test.tsx` (mirror ThemeProvider test: default no-provider, provides values, SagtechUIProvider compose, no-prop back-compat, nesting).
- [ ] Compose in `SagtechUIProvider.tsx` (`locale?`/`dir?`, wrap only when set).
- [ ] Export from `providers/index.ts` + `src/index.ts`.
- [ ] `pnpm typecheck && pnpm test`; commit `feat(providers): add LocaleProvider + useLocale (dir/locale)`.

### Task D.2 — calendar.ts locale helpers + tests
- [ ] `getWeekdayLabels(locale='en-US')` Monday-first via Intl over `Date.UTC(2024,0,1+i)`, memoized.
- [ ] `formatDisplayDate(date, locale='en-US')`, `formatMonthLabel(year, month, locale='en-US')`. Keep `WEEKDAYS`.
- [ ] Tests (regex-based). Commit `feat(datepicker): locale-aware weekday/month helpers (Intl)`.

### Task D.3 — wire DatePicker / DateRangePicker / GanttTimeline
- [ ] Each: `useLocale()` + `locale?` prop (`prop ?? context`, default en-US). DatePicker/DateRangePicker use new helpers; GanttTimeline `undefined`→`locale`.
- [ ] `pnpm test` (existing picker/gantt tests green); commit `feat(dates): consume LocaleProvider for weekday/month labels`.

## Phase E — RTL (logical properties; needs D.1)

**Mapping:** `pl→ps`, `pr→pe`, `ml→ms`, `mr→me`, `left→start`, `right→end`, `border-l→border-s`, `border-r→border-e`. Centered/symmetric/full-bleed/block-axis classes unchanged.
- [ ] Form controls: `Input.tsx` label `left-24px`→`start-24px`; `SelectFakeInput.tsx` chevron `right-[24px]`→`end-[24px]`.
- [ ] In-tree overlays: `Tooltip.tsx`/`Popover.tsx` position maps to logical + `rtl:` arrow border-side swaps + `dir` on wrapper; `Drawer.tsx` logical edges/borders + `ms-auto` + runtime slide-sign via `useLocale().dir` + `dir` on `motion.aside`.
- [ ] Portal overlays: `Toaster.tsx` logical positions + `useLocale()` + `dir` on portal container; `Modal.tsx`/`BottomSheet.tsx` `useLocale()` + `dir` on portal root; DatePicker popover `dir` on portalled node; DateRangePicker `dir` on in-tree root.
- [ ] `src/components/__tests__/rtl.smoke.test.tsx`: Input `start-24px` under `dir=rtl`; DateRangePicker RTL root; **portal-dir guard** (Toaster RTL → body `[dir=rtl]` + `end-16px`).
- [ ] `pnpm test`; commit `feat(rtl): logical-property RTL support for overlays + form controls`.

## Phase G — Motion consolidation Drawer/Sheet (after E)

**Files:** `src/motion/overlayVariants.ts`, `Drawer.tsx`, `Sheet.tsx`
- [ ] Extract `edgeSlideVariants(edge, dir)` (spring 30/300, `hidden/visible/exit`, dir-sign). Modal/SelectDropdownLayout untouched.
- [ ] `pnpm test -t "Drawer" && pnpm test -t "Sheet"` then `pnpm test`; commit `refactor(motion): dedupe Drawer/Sheet edge-slide variants`.

## Phase F — addon-a11y audit run (after C+E)

- [ ] Run audit across stories (`pnpm dev`/`build-storybook`), fix violations. Optionally set `parameters.a11y`. Document pass. Commit any fixes.

## Phase H — raw→semantic migration tail

**Files:** 10 charts, `GanttTimeline`, `DataTable`, `Pagination`, `Combobox`, `NotificationCenter`
- [ ] Apply v1.4 mapping (raw→semantic; accents kept). Charts after Phase B.
- [ ] `pnpm test` + Storybook light smoke; commit per group `refactor(<area>): migrate to semantic color tokens`.

## Phase I — missing Storybook stories

- [ ] `Container`, `Providers/ThemeProvider` (theme-switch demo), `Providers/LocaleProvider` (locale/dir demo), `Motion/presets` demo. `pnpm build-storybook` green. Commit `docs(storybook): add Container/Providers/Motion stories`.

## Phase J — docs + release (NO tag without user go)

- [ ] `docs/I18N.md` + cross-link in `THEMING.md`.
- [ ] `docs/ROADMAP.md`: v1.5 → released + test-count delta.
- [ ] `package.json` `version` → `1.5.0`. Commit `chore(release): v1.5.0 — coverage + a11y/i18n`.
- [ ] Final gate: `pnpm generate:tokens && pnpm typecheck && pnpm lint && pnpm check:contrast && pnpm test && pnpm build && pnpm build-storybook`.
- [ ] **STOP. Await explicit user confirmation before tagging** `v1.5.0` (push triggers irreversible publish).

---

## Ordering / dependencies

`0 → A → B → C → D → E(needs D) → G(after E) → F(after C+E) → H → I → J`. B/C/D mutually independent; dispatch sequentially via subagent-driven loop. Each phase: implementer → spec review → code-quality review → next.
